# test-pie

Webflow Cloud + DevLink code components for NZ Pie Review.

## Setup

1. `npm install`
2. `npx webflow devlink sync` (connect to your Webflow site)
3. `npm run dev`

## Code Components

Add React components in `src/components/` and they'll be available in the Webflow Designer.
